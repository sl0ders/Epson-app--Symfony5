{% trans_default_domain "EpsonProjectTrans" %}
{{ form_start(formSearch) }}
<div class="container">
    <div class="row m-auto">
        <div class="col-md-3 offset-3">
            {{ form_widget(formSearch.company) }}
        </div>
        <div class="col-md-3">
            {{ form_widget(formSearch.submit) }}
        </div>
    </div>
</div>


{{ form_end(formSearch) }}
